{
  "name": "nekokan2-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute DB --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.2.0"
  },
  "devDependencies": {
    "wrangler": "^3.40.0"
  }
}
